[build-system]
requires = ["setuptools >= 61.0"]
build-backend = "setuptools.build_meta"
 
[project]
name = "pyObsFind"
version = "0.0.1"
 
dependencies = [
 "matplotlib",
 "astropy",
 "pandas",
 "astroquery",
 "rich",
 "pylatex",
 "ephem",
]